<nav class="navbar navbar-expand-lg  navbar-dark bg-dark">
    <a class="navbar-brand" href="/">ENDLESS</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
            @*<li class="nav-item active">
                <a class="nav-link" href="/">Home <span class="sr-only">(current)</span></a>
            </li>

            <li class="nav-item">
                <a class="nav-link" href="@Url.Action("Index","TestCache")">Teste Cache</a>
            </li>

            <li class="nav-item">
                <a class="nav-link" href="@Url.Action("Index","Produtos")">Produtos</a>
            </li>

            <li class="nav-item">
                <a class="nav-link" href="@Url.Action("Index","Clientes")">Clientes</a>
            </li>

            <li class="nav-item">
                <a class="nav-link" href="@Url.Action("Index","Pedidos")">Pedidos</a>
            </li>

            <li class="nav-item">
                <a class="nav-link" href="@Url.Action("Index","Usuarios")">Usuários</a>
            </li>*@

            @*<li class="nav-item active">
                <a class="nav-link" href="@Url.Action("About","Home")">About <span class="sr-only">(current)</span></a>
            </li>*@

        </ul>

        <ul class="navbar-nav ml-auto">

            @if (!User.Identity.IsAuthenticated)
            {
                <li class="nav-item">
                    <a class="nav-link" href="@Url.Action("Login","Conta")">Efetuar Login</a>
                </li>
            }
            else
            {
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        @{
                            var user = Newtonsoft.Json.JsonConvert.DeserializeObject<UsuarioViewModel>(User.Identity.Name);
                        }
                        Olá @user.Nome
                    </a>
                    <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                        <a class="dropdown-item" href="@Url.Action("AddEdit","Usuarios", new { id=user.Id})">Meu perfil</a>
                        <a class="dropdown-item" href="#">Trocar minha senha</a>
                        <div class="dropdown-divider"></div>
                        <a class="dropdown-item" href="@Url.Action("LogOut","Conta")">Sair</a>
                    </div>
                </li>
            }
        </ul>

    </div>
</nav>  